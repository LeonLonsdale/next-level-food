export default function NotFound() {
  // const readableSlug = slug.split("-").join(" ");
  return (
    <main className="not-found">
      <h1>We couldn't find that meal.</h1>
      <p>We couldn't find any meals with that name, why don't you add it?</p>
    </main>
  );
}
